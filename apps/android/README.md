# Android client

See the repository root README for setup, pairing, security and deployment.

```sh
flutter pub get
flutter analyze
flutter test
flutter build apk --debug
```

The app requires a reachable, paired HTTPS Bridge. No simulated backend is included in the release application.
